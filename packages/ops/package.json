{
  "name": "@sentinel/ops",
  "version": "0.1.0",
  "description": "Shared Sentinel operations (run orchestration, promotion, git/PR) used by both the CLI and the Studio server.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@sentinel/core": "workspace:*",
    "@octokit/rest": "^21.0.2"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.7.2",
    "vitest": "^4.1.10"
  }
}
